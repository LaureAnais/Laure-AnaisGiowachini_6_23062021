/* const validpassword = require('../models/password');

module.exports = (req, res, next) => {
    // Si le mot de passe est valide 
    // Vérifier que le mot de passe est celui lié à l'email 
    // Si oui = connexion réussie 
    if (validpassword.valide) {
        throw 'Connexion réussie!';
    } else {
        // le mot de passe n'est pas valide 
        // vérifier le mot de passe 
    }
        // le compte n'existe pas, merci de vous inscrire

        catch {
            res.status(401).json({
                error: new Error('Requête non authentifiée!')
              });
        }        
    };
    */